# API Reference

The supported v1 dependency interfaces are the prediction-market facade plus the
JSON worker protocol. The lower-level solver API is still documented below for
research and custom convex-flow work.

For downstream integration, start with `PredictionMarketProblem`,
`solve_prediction_market`, and `compare_prediction_market_families`.

```@index
```

```@autodocs
Modules = [ForecastFlows]
```
